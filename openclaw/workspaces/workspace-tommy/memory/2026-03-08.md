# 2026-03-08
- Donald leak persisted; root cause was conflicting legacy SOUL sections still allowing deliverables in chat. Rewrote Donald SOUL OUTPUT DELIVERABLES + USER OUTPUT CONTRACT to file-only deliverables and strict one-line user replies; tightened IDENTITY and AGENTS; session reset ACK_DONALD_LOCK_V2 + ACK_DONALD_STRICT_ONE_LINE; strict test returned one line only.
- Updated Donald core files directly per Andre request: rewrote workspace-donald/AGENTS.md, IDENTITY.md, and key SOUL sections (USER OUTPUT CONTRACT, ROUTING MATRIX, OUTPUT DELIVERABLES, PRE-SEND OUTPUT GATE) with strict one-line chat lock, path alignment, and metadata-only internal routing. Restarted gateway via SIGUSR1; ready for live test.
- Investigated Donald follow-up leak: custom status prose on user follow-up questions (e.g., "Mark has been notified"). Root cause: one-line lock covered intake but not explicitly follow-up prompts. Patched workspace-donald AGENTS/SOUL/IDENTITY to enforce exact 3-line whitelist for intake + all follow-ups; pushed runtime hotfix to active Telegram Donald session (ACK_DONALD_FOLLOWUP_LOCK), validated with test prompt returning allowed line, then restarted gateway.
- Permanent fix for Donald handoff failure: patched run_helpers.find_existing_success to accept only usable success manifests (status=success + existing raw_transcript_file + nugget_export_file), selecting latest valid artifact set. Patched donald_process_url.py skip branch to carry forward artifact paths from prior usable manifest into new skip manifest (prevents null-path handoffs). Added runtime directive requiring file existence + sessions_send success before claiming Mark notified (ACK_DONALD_HANDOFF_GUARD). Restarted gateway and validated new skip manifest contains existing artifact paths.
