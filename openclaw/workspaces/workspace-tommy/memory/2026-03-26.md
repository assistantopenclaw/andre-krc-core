# 2026-03-26

- 14:00 CDT — Pure Signal cleanup executed archive-first.
  - Moved workspace `tmp_*` artifacts, `krc_scaffold/`, and prior `tmp_audit/` into `hard-archive/2026-03-26T14-00-archive/`.
  - Moved `/home/openclaw_agent_1/.openclaw/scripts/krc/legacy_disabled_2026-03-11/` into the same archive tree.
  - Backed up then removed stale disabled user unit `~/.config/systemd/user/manager-live.service`; reloaded systemd user daemon.
  - Left `openclaw-gateway.service` untouched as the active production gateway.
  - Archived manifest: `hard-archive/2026-03-26T14-00-archive/MANIFEST.txt`.
- 16:15 CDT — Began binary model-routing hardening under GPT-5.4 per Surgery-First rule.
  - Updated `SOUL.md` with binary routing, Surgery-First rule, Mandatory Model Locks, escalation packet rule, and runtime expectation for MiniMax M2.7 default + GPT-5.4 escalation.
  - Updated `IDENTITY.md` with compact binary-lane routing policy and mandatory file/path locks.
  - Verified live runtime binding state in `~/.openclaw/openclaw.json`: Tommy currently remains `openrouter/openai/gpt-5.4` primary with GPT-5.3/GPT-5.2 fallbacks.
  - Verified MiniMax is documented/supported in local OpenClaw docs (`minimax/MiniMax-M2.7`) but not yet configured on this host.
  - Verified no current MiniMax provider/auth is present in `~/.openclaw/openclaw.json`, shell env, or agent auth-profiles, so runtime model binding was intentionally NOT changed yet to avoid bricking Tommy escalation.
- 16:19 CDT — Binary routing implementation status hardened further.
  - Confirmed OpenRouter exposes MiniMax M2.7 as model id `minimax/minimax-m2.7`.
  - User explicitly directed Tommy to use the existing OpenRouter API path for MiniMax instead of standalone MiniMax auth.
  - Routing-policy commit recorded: `245941a` (`Add binary model routing policy for Tommy`).
  - Cleanup/archive commit recorded: `4deac69` (`Archive stale workspace artifacts and remove stale manager-live unit`).
  - Attempted Phase 2 runtime-binding patch to `~/.openclaw/openclaw.json` to set Tommy primary to `minimax/minimax-m2.7` with GPT-5.4 fallback and restart gateway.
  - That exec was interrupted by SIGTERM before post-restart verification completed, so final runtime binding state is UNCONFIRMED and must be verified first thing next session before claiming MiniMax default is live.
- 16:33 CDT — Weighted Ink / Tommy binary routing remains actively in progress.
  - Core persona routing patch is complete in `SOUL.md` and `IDENTITY.md`.
  - Live Tommy config in `~/.openclaw/openclaw.json` is now set to `primary: minimax/minimax-m2.7` with fallbacks `openrouter/openai/gpt-5.4`, `openrouter/openai/gpt-5.3-codex`, `openai/gpt-5.2`.
  - Root cause fixed: prior bad model id `openrouter/minimax/minimax-m2.7` was corrected to `minimax/minimax-m2.7`.
  - Backup created before incision: `~/.openclaw/openclaw.json.pre-minimax-fix-1764 CDT.bak`.
  - Gateway is running healthy after the config correction.
  - Remaining blocker: current Telegram session context is past the 200k token limit, so MiniMax cannot be fairly validated here.
  - Required next step: start a fresh Tommy chat/session, then run at least 3 clean routing tests and verify MiniMax is used directly with no fallback to GPT-5.4 on normal low-risk runs.
  - Do not declare final routing validation complete until the fresh-session test passes.
