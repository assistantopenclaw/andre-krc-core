# 2026-02-28


## Tasklist memory system hardened
- Implemented OPEN vs ARCHIVE idea storage under tasklist workspace:
  - OPEN: /home/openclaw_agent_1/.openclaw/workspace-tasklist/ideas/open/*.json
  - ARCHIVE: /home/openclaw_agent_1/.openclaw/workspace-tasklist/ideas/archive/*.json
- Added close flow requiring A/B/C options, plus undo-last support.
- Enforced capture-default: for “remember …” without date/time, save as OPEN and do NOT ask scheduling questions.
- Added full_idea storage (internal) + one-line description for user-facing lists.
- Enforced numbered list output `1. 2. 3.`.
- Added persistence guarantee: every remember/close/undo event appends to tasklist memory/YYYY-MM-DD.md (append-only) in addition to structured JSON.

## Cron routing fix
- Fixed 9am digest cron to send from Tasklist (agentId=tasklist, sessionKey=agent:tasklist:telegram:direct:8434682557) instead of Tommy.

## Tasklist model churn + resolution
- Multiple model updates attempted; gemini-2.5-flash-lite triggered intermittent “Unhandled stop reason: error” in embedded runs.
- Temporarily rolled to gemini-2.5-flash for stability, then user requested return to gemini-2.5-flash-lite (primary) w/ gpt-5-nano fallback.
- Updated tasklist SOUL/guardrails so assigned model messaging matches config.

## Research analytics scope updates
- Updated research cron schedule to only 2 pulls/day: 11:00 AM + 11:00 PM America/Chicago.
- Removed YouTube scope (Meta-only: IG + FB under one Meta Business account).
- Locked role split: Tommy builds infra; Research runs API pulls + analytics only.

## Jimmy/Donald core-file clarity fixes
- Added Jimmy READY definition + Drive upload failure escalation to Tommy (no “ready” DM until Drive upload + link + manifest updated).
- Added Donald failure escalation payload requirement: RUN_ID + step + error + log path.
- Added examples to help Jimmy comply with “no dashes” punctuation rule.

## Google Drive / Google Docs output improvements
- Converted finished-content uploads to native Google Docs (`gog drive upload ... --convert-to doc`).
- Implemented human-readable doc titles:
  - New convention: `RUN_ID: <first caption line>` (colon used to avoid dash rule and improve scanability).
  - Updated pipeline helper: ~/.openclaw/scripts/krc/run_helpers.py adds infer_drive_doc_name(); ensure Path import present.
  - Updated Jimmy processor: ~/.openclaw/scripts/krc/jimmy_process_export.py uploads with `--name "<generated>"` and records drive.file_id/link.
  - Updated Jimmy HEARTBEAT.md to include the command with --name.
- Renamed recent existing docs in Drive folder using `gog drive rename <fileId> <newName>` (note: gog has rename command; no drive update subcommand).

## Ops notes
- rg (ripgrep) not installed in environment; used grep as fallback.
