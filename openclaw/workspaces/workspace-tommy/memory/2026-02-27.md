
# 2026-02-27 Tommy Daily Log

## Gateway Token Mismatch — Root Cause + Fix (12:55 PM CT)
- Root cause: `openclaw.json` had placeholder token `<PASTE_GATEWAY_TOKEN_HERE>` instead of real token
- Real token lives in systemd env var `OPENCLAW_GATEWAY_TOKEN` in user service file
- Fixed by syncing config file token to match systemd env var
- Full systemd restart (`systemctl --user restart openclaw-gateway`) required — SIGUSR1 doesn't resync tokens
- Key learning: direct config file writes can clobber the gateway token if config.patch uses `__OPENCLAW_REDACTED__` placeholder

## Alex Model Change
- Changed Alex primary: Claude Opus 4.6 → GPT 5.2
- Fallback: Claude Opus 4.6

## SOUL.md Model Identity (all 5 agents)
- Added "Assigned Model (DO NOT INHERIT DEFAULT)" section to every agent's SOUL.md
- Includes primary + fallback(s) and instruction to flag immediately if running wrong model

## Elevated Exec — Tommy Only
- Global: tools.elevated.enabled=true, allowFrom telegram 8434682557
- Main, Research, Tasklist, Alex: tools.elevated.enabled=false
- Tommy: inherits global (enabled)

## Model Drift Check Cron
- Script: /home/openclaw_agent_1/.openclaw/scripts/check_agent_models.py
- Checks all 5 agents have correct explicit primary models
- Cron job: model-drift-check, runs every 6 hours on Tasklist (Nano)
- If drift detected, Tasklist pings Tommy to fix
- If clean: ALL_CLEAR, no notification, no cost

## Cron Job Fixes
- daily-research-sweep: model changed from openrouter/auto → openrouter/google/gemini-3-flash
- Both cron jobs: delivery set to bestEffort=true to prevent hard failures

## OpenClaw Version
- Already on 2026.2.26 (current latest). No update needed.

## Active Cron Jobs (3 total)
1. daily-local-backup — 3 AM CT daily — Tasklist (Nano)
2. daily-research-sweep — 12 PM CT daily — Research (Gemini 3 Flash)
3. model-drift-check — every 6 hours — Tasklist (Nano)

## KRC Pipeline + Google Drive OAuth Completion (5:16–5:20 PM CT)
- Received OAuth client JSON via inbound media and saved to `/home/openclaw_agent_1/.openclaw/credentials/google/client_secret_drive.json`
- Registered credentials with `gog auth credentials` (stored at `~/.config/gogcli/credentials.json`)
- Initial OAuth callback failed token write in headless mode: `no TTY available ... set GOG_KEYRING_PASSWORD`
- Switched to remote OAuth flow (`gog auth add --remote --step 1/2`) and completed step 2 using redirect URL provided by Andre
- Verified auth: `assistant@kingdomrelationshipcoaching.com` has Drive scope via gog
- Created Drive folder `OpenClaw Finished Content` (folderId: `1NOCxQ2K1S2vM07hGHc0xEqTrsl2Ub1tv`)
- Uploaded test artifact: `20260227_162237_100680ad__finished_content.md`
- Test file URL: `https://drive.google.com/file/d/1bTaEFjj9UqcNzF2AUNTNRqGXhn9gSBxc/view?usp=drivesdk`

## Donald Browser Access + Transcript Fetch Reliability (5:28–5:34 PM CT)
- Enabled Gateway `browser` block in config to expose browser tool to agents
- Channel probes passed after restart (Donald/Jimmy/Tommy/Tasklist/default all healthy)
- Host browser start test failed: `No supported browser found (Chrome/Brave/Edge/Chromium)` on this host
- Implication: Donald can only auto-pull Fathom transcript if either:
  1) host has supported Chromium-based browser installed, or
  2) user provides transcript export (TXT/SRT/VTT) for ingestion

## Model ID Incident + Recovery Work (5:30–6:23 PM CT)
- Validated model ID confusion against OpenRouter model list endpoint (`/api/v1/models`)
- Corrected Donald primary to OpenRouter-prefixed ID (`openrouter/google/gemini-3-flash-preview`)
- Detected broader prefix drift across multiple agents (some entries had `openai/`, `google/`, `anthropic/` without `openrouter/`)
- Applied recovery pass to restore OpenRouter-prefixed model IDs across agent primaries/fallbacks and restarted gateway
- Follow-up required: run full post-restart model sanity check + `/status` ping for each agent to confirm routing on OpenRouter only

## Major Ops Day Summary — Durable Context for Follow-up (10:49 PM CT)
- Completed KRC 2-agent pipeline stabilization work (Donald extractor + Jimmy content creator) and resolved multiple production blockers.

### Pipeline Architecture / Routing
- Confirmed and enforced Donald -> Jimmy internal handoff path.
- Enabled cross-agent session visibility (`tools.sessions.visibility=all`) for reliable session targeting.
- Standardized internal routing to explicit session keys when needed.
- Fixed vault mismatch issue (Donald wrote to local workspace path while Jimmy read shared vault):
  - Recovered missing run artifacts into shared vault.
  - Symlinked both agent workspaces to shared vault path:
    - `workspace-donald/krc_vault -> /home/openclaw_agent_1/krc_vault`
    - `workspace-jimmy/krc_vault -> /home/openclaw_agent_1/krc_vault`

### Google Integration
- Drive OAuth completed for `assistant@kingdomrelationshipcoaching.com` in headless mode via gog remote step1/step2.
- Keyring non-TTY issue solved with `GOG_KEYRING_PASSWORD`.
- Created Drive folder: `OpenClaw Finished Content` (ID persisted in prior logs).
- Verified Drive upload success with test file.
- Later expanded auth scope to `drive,gmail`; verified Gmail scope live.
- Jimmy confirmed ability to send Gmail notifications once recipient email is provided.

### Donald Fetch Reliability
- Implemented API-first Fathom retrieval in `/home/openclaw_agent_1/.openclaw/scripts/krc/donald_process_url.py`:
  - Match meeting via `GET https://api.fathom.ai/external/v1/meetings` by `share_url`
  - Pull transcript via `GET https://api.fathom.ai/external/v1/recordings/{recording_id}/transcript`
  - Fallback to curl page fetch only if API path fails.
- Verified successful transcript pulls on real share links with successful manifests/logs.
- Corrected API endpoint confusion: use `api.fathom.ai` (not `api.fathom.video`).

### Donald Behavior Policies (critical)
- Updated Donald docs/persona files to enforce:
  - Never ask Andre for manual transcript paste as first response.
  - Escalate fetch blockers to Tommy only.
  - API-first handling for Fathom links.
  - Nugget output no longer constrained to small fixed counts.
  - Include all high-signal nuggets with overall score >= 6/10 (ranked).
- Donald acknowledged policy updates in-session (`ACK_DONALD_POLICY_UPDATED`, `ACK_DONALD_NO_USER_BLOCKERS`).

### Model / Gateway Stability
- Repaired prior model ID drift and restored OpenRouter-prefixed model IDs where required.
- Updated requested fallbacks:
  - Tasklist fallback -> `openrouter/google/gemini-2.5-flash`
  - Alex fallback -> `openrouter/anthropic/claude-sonnet-4.6`
- Re-ran health checks (`doctor`, `status --deep`, `security audit --deep`, `update status`).
- Applied hardening fixes where safe (credentials dir perms, ineffective denyCommands cleanup).

### Savepoint / Disaster Recovery
- Created full “video-game savepoint” backup:
  - `/home/openclaw_agent_1/savepoints/20260227_222254`
  - archive + sha256
  - `latest` symlink pointer
- Built one-command restore script:
  - `/home/openclaw_agent_1/restore_savepoint.sh`
  - Alias: `restore_savepoint`
  - Recovery command: `restore_savepoint latest --yes`

### Business Clone Kit (requested deliverable)
- Created static clone kit: `/home/openclaw_agent_1/openclaw business blueprint V1`
- Includes:
  - `export_clone_bundle.sh`
  - `import_clone_bundle.sh`
  - `post_import_checklist.sh`
  - `README.md`
  - `openclaw-business-blueprint-v1.tar.gz` (+ `.sha256`)
- Purpose: replicate current OpenClaw business system to another machine (dad’s PC) with rekey-only setup.

### User Intent to Resume in New Chat
- Andre plans to resume in a new chat in a few days to run clone/deploy process for dad’s environment.
- Must continue from this context quickly with no re-discovery overhead.
- Priority on next session: guided transfer + import on dad’s Windows machine (via Jump Desktop), then rekey + smoke test.
